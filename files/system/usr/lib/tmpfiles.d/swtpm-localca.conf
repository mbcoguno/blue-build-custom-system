# swtpm CA statedir for libvirt-managed vTPMs (e.g. Windows 11 guests).
# Fedora's swtpm package doesn't ship this dir; `ujust setup-virtualization`
# creates it at runtime. We declare it via tmpfiles so it exists on first boot.
d /var/lib/swtpm-localca 0750 tss tss - -
